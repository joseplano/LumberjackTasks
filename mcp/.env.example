PORT=5000
BACKEND_URL=http://localhost:4000
# Option A: static JWT (takes precedence; note backend tokens expire after 12h)
MCP_JWT=
# Option B: credentials for automatic login + re-login on expiry
MCP_EMAIL=
MCP_PASSWORD=
